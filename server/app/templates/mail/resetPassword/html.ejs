Hello <%= username %>,<br>
<br>
<strong>We have just received a request to reset your password</strong> for your account at <a href="<%- config.host %>"><%= config.siteName %></a>.<br>
<br>
If you forgot your password and need to reset it, follow the link below:<br>
<% var resetLink = config.host + '/#/resetPassword/' + username + '/' + token; %>
<a href="<%- resetLink %>"><%= resetLink %></a><br>
<br>
If you did not request this password reset, just ignore this email; the link will expire after 24 hours.<br>
<br>
Cheers,<br>
<%= config.siteName %> team
